1. git init
Yeni bir Git deposu oluşturur. Bu komut, çalıştırıldığı dizinde bir .git klasörü oluşturur. Bu klasör, Git'in tüm deposuyla ilgili bilgilerini ve tarihçesini içerir.

git init

2. git clone
Uzak bir Git deposundaki bir projeyi yerel makinenize kopyalar. Bu komut, uzak depodaki tüm dosyaları ve tarihçeyi yerel dizininize kopyalar.

git clone <repo_url>

3. git add
Değişiklikleri bir sonraki commit'e hazırlar. Yeni dosyaları izlemeye alır veya değişen dosyaları staging alanına ekler.

git add <dosya_adı>

4. git commit
Staging alanında bulunan değişiklikleri yerel depoya kaydeder. Her commit, bir commit mesajı ile belirtilir.

git commit -m "commit_mesajı"

5. git push
Yerel depodaki değişiklikleri uzak depoya gönderir.

git push <uzak> <dallar>

6. git pull
Uzak depodaki değişiklikleri yerel depoya çeker ve birleştirir.

git pull <uzak> <dallar>

7. git status
Çalışma dizinindeki dosyaların durumunu gösterir. Hangi dosyaların değiştirildiğini, staging alanında olduğunu veya commit edilmeyi beklediğini gösterir.

git status

8. git branch
Mevcut dalları listeler. Hangi daldaysanız o dalın yanında "*" işareti ile belirtilir.

git branch

9. git checkout
Belirli bir dala geçiş yapar veya dosyaları geri yükler. Dalı değiştirmek için kullanılır veya belirli bir commit veya dalın durumuna geri dönmek için kullanılabilir.

git checkout <dal_adı>

10. git merge
İki farklı daldaki değişiklikleri birleştirir. Birleştirme işlemi, iki dalın son ortak commit noktasını bulur ve değişiklikleri birleştirir.

git merge <dal_adı>

Bu komutlar Git'in temel işlevlerini kapsar. Git hakkında daha fazla bilgi edinmek için Git belgelerine veya çevrimiçi kaynaklara başvurabilirsiniz.
